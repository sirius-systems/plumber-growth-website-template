{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "WebWorker"],
    "types": ["@cloudflare/workers-types"],
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "forceConsistentCasingInFileNames": true,
    "noUncheckedIndexedAccess": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "//": "Root at functions/** only. tsc transitively type-checks the server lib it imports (lib/forms/schemas, lib/ghl/*, config/services) under the Workers lib, while client-only files (React hooks using window/DOM) are never reached and stay in the app project.",
  "include": ["functions/**/*.ts"],
  "exclude": ["node_modules", "out", ".next"]
}
